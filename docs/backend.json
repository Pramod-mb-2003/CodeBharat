{
  "entities": {
    "UserProfile": {
      "title": "User Profile",
      "description": "Represents a user's profile information.",
      "type": "object",
      "properties": {
        "userId": {
          "type": "string",
          "description": "The unique user ID for the student."
        },
        "email": {
          "type": "string",
          "format": "email",
          "description": "The user's email address, used for authentication."
        },
        "password": {
          "type": "string",
          "description": "The user's password. Note: Storing plain text passwords is a security risk."
        },
        "progress": {
          "type": "object",
          "description": "The user's progress in different interest categories."
        },
        "interests": {
          "type": "array",
          "description": "The user's selected interests.",
          "items": {
            "type": "string"
          }
        }
      },
      "required": ["userId", "email", "password"]
    },
    "UserCredits": {
      "title": "User Credits",
      "description": "Represents the credits a user has accumulated.",
      "type": "object",
      "properties": {
        "credits": {
          "type": "number",
          "description": "The number of credits the user has."
        }
      }
    }
  },
  "auth": {
    "providers": [
      "email-password"
    ]
  },
  "firestore": {
    "/users/{userId}": {
      "schema": {
        "$ref": "#/backend/entities/UserProfile"
      },
      "description": "Stores public information about a user."
    },
    "/user_credits/{userId}": {
      "schema": {
        "$ref": "#/backend/entities/UserCredits"
      },
      "description": "Stores the credits for a specific user."
    }
  }
}
